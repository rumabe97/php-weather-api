<?php
abstract class LoginAbstractService
{
    // abstract function createUser($user);

    // abstract function updateUser($id, $user);

    // abstract function deleteUser($id);

    // abstract function getUserById($id);

    // abstract function getUsers();

    abstract function login($email, $password);
}
